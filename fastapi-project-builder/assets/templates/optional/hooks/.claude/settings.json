{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif [ -x venv/bin/python ]; then PY=venv/bin/python; elif [ -x env/bin/python ]; then PY=env/bin/python; else echo \"未找到项目本地虚拟环境，请先创建 .venv、venv 或 env\" >&2; exit 1; fi; \"$PY\" scripts/hooks/apifox_sync_guard.py'"
          }
        ]
      }
    ]
  }
}
